{
  "name": "openhealth",
  "version": "1.0.0",
  "description": "OpenHealth Healthcare Discovery, Bed Inventory & Clinical Transparency Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan.bold,green.bold\" --kill-others \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "node start-dev.js",
    "dev:runner": "node start-dev.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "healthcare",
    "openhealth",
    "hospital",
    "beds",
    "transparency",
    "ayushman"
  ],
  "author": "OpenHealth Team",
  "license": "MIT"
}
